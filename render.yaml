services:
  - type: web
    name: hk-webflow-ai-chatbot
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      